# Stage Run Record — 2026-08-22 17:00 (Stage 2)

## Identity

- Stage: `stage2` (cron `0 17 * * *`, triggered 2026-08-22T17:04:36Z)
- Policy: `pr-lifecycle-v1.4`; Stage 2 never merges, approves, closes, or marks
  ready. Salvage replacements stay **draft**.
- Ledger: fetched rev **11** blob `a543e6c82abee728426d391ba449bd9a93dfdd8a`;
  CAS **11 → 12** commit `411984a73e0d786e27064a4f36413cdfd7dc4222`, blob
  `61f895c52bfae47b86087a457c49e79bc66e1adf` on
  `automation/pr-lifecycle-ledger:pr-lifecycle-ledger.yaml`
- Write primitive: `github_contents_api`. PUT used branch
  `automation/pr-lifecycle-ledger` with live blob SHA precondition. Refetch
  schema+records VALID. Calibration untouched (`REPORT_ONLY`, count **2**,
  `policy_revision: pr-lifecycle-v1.4`).
- Work items consumed: `s2-20260822-esp-1514-path-import`. Cap 5; completed
  **1** (only complete unexpired Stage-2-owned item). Remaining
  `stage2_work_items`: **[]**.
- `python3 scripts/validate_pr_lifecycle_artifacts.py` still fails on
  `docs/cursor-automations/exports/daily-pr-review.json` prompt wrap vs source
  (pre-existing on `main`). Ledger-only `validate_schema` +
  `validate_runtime_records` PASS. Stage 2 did not edit prompt/export files.

## Outcomes

| Repo                    | Old PR | Disposition                                                               | New PR                                                                  | Notes                                                                                                                                                                                                                                 |
| ----------------------- | -----: | ------------------------------------------------------------------------- | ----------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| email-security-pipeline |   1514 | SALVAGE (opened draft; converted back after `linear-code[bot]` marked ready) | [#1515](https://github.com/abhimehro/email-security-pipeline/pull/1515) | Original ready CLEAN; left open. Opened from current main `e009e592…` (`automation` + `cursor-agent/` = BOT). Create-time `isDraft=true`; ready at 17:14:07Z; `convert_to_draft` 17:16:06Z before ledger write. `python3 -m pytest` PASS. |

- Salvage drafts opened: **1** (#1515)
- Infra-fix drafts: **0**
- Closed via API: **0**
- Autonomous merges / approvals / ready conversions: **0** (Stage 2 converted
  #1515 **to draft**, never to ready)
- `request_reviewers`: skipped
- New lessons: **0**
- Ledger events: `evt-s2-20260822-emailsecuri-1514-a/h`,
  `evt-s2-20260822-emailsecuri-1515-h`
- New item:
  `abhimehro/email-security-pipeline#1515@01e5600238a7acfb6b4317ad39e8c6bf02a4bfa7`
  BOT / FEATURE / ROUTINE / HOLD_EVIDENCE / `STAGE3_RECONCILIATION` /
  `next_owner` stage1 (GITHUB_SQUASH)

## Verification

- ESP #1515 allowed path only:
  `.github/scripts/repository_automation_tasks.py` (+1 line
  `from pathlib import Path` after `import re`). Prohibited paths untouched.
- Named test: `python3 -m pytest` → **754 passed**, 40 subtests, exit 0
  (re-run after draft open: 7.62s).
- Runtime: `Path` is bound at module import, not only under
  `typing.TYPE_CHECKING`.
- Re-read after create: `isDraft=true`, head
  `01e5600238a7acfb6b4317ad39e8c6bf02a4bfa7`, base
  `e009e5923860f5b504f6e179ad2380efe514bf4d`. Timeline:
  `linear-code[bot]` `ready_for_review` at 2026-08-22T17:14:07Z; Stage 2
  `convert_to_draft` at 2026-08-22T17:16:06Z; post-convert `draft=true`.
- Original #1514 left OPEN. Do not close because a replacement exists.
- CodeScene MCP was unavailable (`serverStatus=error`). Original #1514
  CodeScene check already **success**. Replacement stays HOLD_EVIDENCE until
  Stage 1 sees required checks.

## Handoff

1. Stage 1 later: re-ingest **#1515** as routine FEATURE; GITHUB_SQUASH only if
   every routine predicate passes. Do not mark ready. Do not convert original
   **#1514**.
2. Stage 3: original **#1514** is HOLD_CANONICAL (competing salvage). Keep OPEN.
   Hydro **#543** and Seatek **#708** stay human from prior runs. ctrld **#1161**
   remains HOLD_EVIDENCE (0fv / 0gm); do not recreate `display.py`.
3. Do not squash TYPE_CHECKING-only #1514. Do not request reviewers on #1515.

## Unused capacity

Only one complete unexpired Stage-2-owned work item existed. Cap remainder is
not a failed run. Did not invent queue from Stage 3 remainder markdown. Did not
retry ctrld #1161 or rpce Swift HOLD_PLATFORM items.
