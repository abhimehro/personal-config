# SECURITY: Example only — REPLACE_WITH_* is not a live token. Copy to .env and
# fill locally; never commit the filled file.
# Used by morning-brief.py to fetch daily reading highlights
# Obtain your token from: https://readwise.io/access_token
READWISE_TOKEN=REPLACE_WITH_YOUR_READWISE_TOKEN
