# Optional Configuration for DNS Gaming Mode Scripts
# These variables allow customization without editing the scripts

# Control D service labels (auto-discovered if not set)
# CONTROLD_LABELS="com.controld.ctrld ctrld"

# Logging directory
LOG_DIR="$HOME/Library/Logs/dns-mode"

# Backup directory 
BACKUP_DIR="$HOME/Documents/dev/personal-config/dns-setup/backups"

# Override network services to manage (auto-detected if empty)
# FORCE_SERVICES="Wi-Fi Ethernet"

# Control D DNS servers for normal browsing mode (local Control D instance)
# Uses local Control D proxy with full security profile
CONTROL_D_NORMAL_DNS="127.0.0.1"

# Gaming-optimized Control D DNS servers (for Proton VPN custom DNS)
# Uses your custom Control D gaming profile with GeForce NOW optimizations
# Set these to your gaming-optimized Control D legacy DNS servers
CONTROL_D_GAMING_DNS="********** **********"

# Gaming mode approach: 'proton-custom' (use custom DNS in Proton VPN) or 'legacy' (original approach)
GAMING_MODE_TYPE="proton-custom"
