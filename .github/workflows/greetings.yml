name: Disabled Greetings
on:
  workflow_dispatch:
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled per modernization plan. Functionality fully shifted to agentic PR reviewers."
