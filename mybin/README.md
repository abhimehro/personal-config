# mybin notes

- `sync-launchagents` is available on `$PATH` via
  `~/.local/bin/sync-launchagents`; it hard-copies repo LaunchAgent plists into
  `~/Library/LaunchAgents/`, reloads changed services, and can be checked with
  `sync-launchagents --status`.
