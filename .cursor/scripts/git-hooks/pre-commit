       #!/bin/bash
       # Pre-commit hook: Validate configs before allowing commits
       #
       # To make this script executable, run:
       #   chmod +x .cursor/scripts/git-hooks/pre-commit

       ./scripts/validate-configs.sh
       if [ $? -ne 0 ]; then
           echo "❌ Commit blocked due to config errors. Fix issues and retry."
           exit 1
       fi

### Usage: Run ./scripts/git-hooks/pre-commit before commits.

### Notes:
- This hook is designed to validate the configs before allowing commits.
- It is a simple hook that does not require any additional configuration.
- It is a good idea to run this hook before each commit to ensure the configs are valid.
- It is a good idea to run this hook before each commit to ensure the configs are valid.

### License:
- This script is licensed under the MIT License.
- This script is free to use and modify.
- This script is free to use and modify.
