# Agent zsh interactive extras only
# Non-interactive -c runs should rely on .zshenv alone.

# Aliases (interactive convenience; avoid depending on these in agent scripts)
alias ll='ls -la'
alias gs='git status -sb'
alias ..='cd ..'

# Clearer prompt when a human drops into agent-zsh
PS1='%n@agent:%~%# '
