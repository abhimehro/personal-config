🎯 **What:** Added coverage for `extract_domains_from_file`, including success and failure paths.

📊 **Coverage:**
- Happy path (valid extraction)
- Skipped missing `PK` entries
- Missing `rules` key
- Empty input/rules
- Invalid JSON shape
- Missing file

✨ **Result:** Better confidence in file parsing behavior and safer refactoring around domain extraction.

_Note regarding code review:_ The automated reviewer flagged that `return domains` was missing and needed to be added to the source file. Confirmed `return domains` is already present in the current source of `adguard/scripts/extract_domains.py` (which is why the tests are passing locally). No functional fix was needed there, only test coverage. The reviewer likely evaluated only the patch and not the current file state.
