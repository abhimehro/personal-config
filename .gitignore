# Personal Configuration Repository - .gitignore
# Comprehensive ignore patterns for secure personal configuration management

# =============================================================================
# SECURITY - Never commit these
# =============================================================================

# SSH Keys and sensitive authentication
*.key
*.pem
*_rsa
*_dsa
*_ed25519
*_ecdsa
.ssh/id_*
.ssh/known_hosts
.ssh/authorized_keys

# Environment files and secrets
.env
.env.*
.secrets/
configs/.gemini/oauth_creds.json
.sensitive-paths.txt
config.json
*.conf
*.ini

# VPN configuration files
*.ovpn
*.vpn
vpn.conf

# DNS configuration with potential IPs/domains
ctrld.toml
*.dns

# =============================================================================
# SYSTEM FILES
# =============================================================================

# macOS system files
.DS_Store
.AppleDouble
.LSOverride
Icon

._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# =============================================================================
# DEVELOPMENT
# =============================================================================

# Editor directories and files
.idea/
.vscode/settings.json
.vscode/launch.json
*.sublime-workspace
*.sublime-project

# Python
__pycache__/
*.py[cod]
*$py.class
.ipynb_checkpoints
*.egg-info/
dist/
build/
.pytest_cache/

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
.yarn-integrity
package-lock.json
yarn.lock

# Ruby
.bundle/
vendor/bundle
Gemfile.lock

# =============================================================================
# LOGS & TEMPORARY FILES
# =============================================================================

# Log files
*.log
log/
logs/
**/var/log/

# Cache directories
.cache/
cache/

# Temporary files
*.tmp
*.temp
*.swp
*.swo
*~
.backup
.orig
*mcp-config*.local.json
mcp-configs/*.local.json
mcp-configs/servers.local.json
.mcp-config.local.json

# History files
.bash_history
.zsh_history
.fish_history
.local/share/fish/fish_history
**/.local/share/fish/fish_history

# =============================================================================
# PROJECT SPECIFIC
# =============================================================================

# Backup configurations (keep structure, ignore content)
dns-setup/backups/*/
!dns-setup/backups/.gitkeep

# Local development overrides
configs/local/
*.local

# Testing artifacts
test-results/
coverage/
.coverage

# =============================================================================
# EXCEPTIONS - Allow these specific files
# =============================================================================

# Exclude terminal history files FIRST (before !*.txt exception)
# These may contain command history and should not be committed
.cursor/projects/**/terminals/*.txt
.cursor/**/terminals/*.txt

# Allow example configurations
!configs/*.example.conf
!configs/*.example.ini
!configs/*.example.json
!.env.example

# Allow public SSH configs (no keys)
!configs/ssh/config
!configs/ssh/agent.toml

# Allow versioned documentation
!docs/
!*.md
!*.txt

# BUT exclude terminal history files (they may contain secrets)
.cursor/projects/**/terminals/*.txt
.cursor/**/terminals/*.txt

# Allow scripts (but not sensitive ones)
!scripts/
!*.sh

# Allow test files
!tests/

# =============================================================================
# SPECIAL PATTERNS
# =============================================================================

# Ignore any file with 'secret', 'private', or 'sensitive' in the name
*secret*
*private*
*sensitive*
*credentials*
*password*
*token*
*_key

# But allow documentation about these topics
!*secret*.md
!*private*.md
!*sensitive*.md

# Cursor editor (keep repo-managed config; ignore machine-local state)
.cursor/ide_state.json
.cursor/argv.json
.cursor/ai-tracking/
.cursor/extensions/
.cursor/projects/

# =============================================================================
# FISH (Option A: track source config + plugin list; ignore Fisher-managed files)
# =============================================================================
# Fish universal variables are machine-local and frequently updated (e.g., Hydro’s per-session vars).
configs/.config/fish/fish_variables

# Local/backup files
configs/.config/fish/config.fish.bak

# Fisher-installed plugin files (generated on each machine via `fisher update`)
configs/.config/fish/conf.d/*
configs/.config/fish/completions/*
configs/.config/fish/functions/*

# Keep repo-managed Fish sources
!configs/.config/fish/config.fish
!configs/.config/fish/fish_plugins
!configs/.config/fish/RESTORE_CUSTOMIZATIONS.md
!configs/.config/fish/functions/nm-*.fish
!configs/.config/fish/functions/fish_greeting.fish
!configs/.config/fish/functions/*.backup

# Local-only / machine-generated files
* (1)
* (1).*
media-streaming/backup/*.backup
media-streaming/backup/rclone.conf.backup
!media-streaming/configs/media-credentials.example

# Secrets
controld-system/profiles.env
